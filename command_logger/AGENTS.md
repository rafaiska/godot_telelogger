This is a Python Django + rest_framework API. There should be a virtual environment set up in .venv. In case it does not exist, we must execute the following commands to create it:

- $ python3 -m venv .venv
- $ .venv/bin/pip install -r requirements.txt

All tests must be executed using this virtual environment to isolate it from the OS Python executables and libraries.